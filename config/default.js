/*module.exports = {
  auth: {
    argon: {
      saltLength: 16, //bytes
      hashLength: 32, //bytes
      timeCost: 6,   // aantal iteraties
      memoryCost: 2**17, // 128 MiB (i = rekenen met 1024), maximaal geheugen dat gebruikt mag worden
    },

    jwt: {
      issuer: 'motor.hogent.be',  // gaf de token uit
      audience: 'motor.hogent.be', // voor wie de token geldig is (een applicatie)
      expirationInterval: 60 * 60 * 1000, // 1 hour in milliseconds
      secret: 'eenveeltemoeilijksecretdatniemandooitzalradenandersisdesitegehacked',
    }
  }
}; */