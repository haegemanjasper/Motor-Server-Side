const { tables } = require('..');

module.exports = {
  seed: async (knex) => {
    await knex(tables.klant).delete();

    await knex(tables.klant).insert([
      {
        id: 1,
        name: 'Jasper Haegeman',
      },
      {
        id: 2,
        name: 'Dimitri Dewolf',
      },
      {
        id: 3,
        name: 'Didier De Maeyer',
      },
    ]);
  },
};
